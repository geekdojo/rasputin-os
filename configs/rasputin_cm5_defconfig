# Rasputin OS — Raspberry Pi CM5 (arm64).
#
# STARTER defconfig. The canonical way to evolve this is:
#   make BR2_EXTERNAL=$PWD O=output/cm5 rasputin_cm5_defconfig
#   make BR2_EXTERNAL=$PWD O=output/cm5 menuconfig      # refine
#   make BR2_EXTERNAL=$PWD O=output/cm5 savedefconfig   # write back here
#
# This file lists the load-bearing options; menuconfig will pull in deps.

# --- target arch ---
BR2_aarch64=y
BR2_cortex_a76=y

# --- toolchain / init / libc ---
BR2_TOOLCHAIN_BUILDROOT_GLIBC=y
BR2_INIT_SYSTEMD=y
BR2_SYSTEM_DHCP="eth0"
BR2_TARGET_GENERIC_HOSTNAME="rasputin"
BR2_TARGET_GENERIC_ISSUE="Rasputin OS"

# --- kernel (RPi) ---
# TODO(scaffold): pin the RPi 6.x kernel + CM5 dtb. Placeholder defaults;
# refine via menuconfig against the RPi BSP.
BR2_LINUX_KERNEL=y

# --- RPi firmware ---
BR2_PACKAGE_RPI_FIRMWARE=y

# --- A/B + RAUC ---
BR2_PACKAGE_RAUC=y
BR2_PACKAGE_HOST_RAUC=y
BR2_PACKAGE_SQUASHFS=y
# verity bundle support (dm-verity) — control-plane/updates.md §2
BR2_PACKAGE_CRYPTSETUP=y

# --- app primitive ---
BR2_PACKAGE_DOCKER_ENGINE=y
BR2_PACKAGE_DOCKER_CLI=y

# --- Rasputin packages (this external tree) ---
BR2_PACKAGE_RASPUTIN_AGENT=y
BR2_PACKAGE_RASPUTIN_API=y

# --- rootfs: squashfs (read-only, verity-friendly) ---
BR2_TARGET_ROOTFS_SQUASHFS=y
BR2_TARGET_ROOTFS_SQUASHFS4_XZ=y

# --- overlay + hooks ---
BR2_ROOTFS_OVERLAY="$(BR2_EXTERNAL_RASPUTIN_PATH)/board/rasputin/common/rootfs-overlay"
BR2_ROOTFS_POST_BUILD_SCRIPT="$(BR2_EXTERNAL_RASPUTIN_PATH)/board/rasputin/common/post-build.sh"
BR2_ROOTFS_POST_IMAGE_SCRIPT="$(BR2_EXTERNAL_RASPUTIN_PATH)/board/rasputin/common/post-image.sh"
BR2_ROOTFS_POST_SCRIPT_ARGS="cm5"
