# Rasputin OS — LattePanda Mu / Intel N100 (amd64).
#
# STARTER defconfig — same evolve loop as the CM5 config:
#   make BR2_EXTERNAL=$PWD O=output/n100 rasputin_n100_defconfig
#   make BR2_EXTERNAL=$PWD O=output/n100 menuconfig
#   make BR2_EXTERNAL=$PWD O=output/n100 savedefconfig

# --- target arch ---
BR2_x86_64=y
BR2_x86_corei7=y

# --- toolchain / init / libc ---
BR2_TOOLCHAIN_BUILDROOT_GLIBC=y
BR2_INIT_SYSTEMD=y
BR2_SYSTEM_DHCP="eth0"
BR2_TARGET_GENERIC_HOSTNAME="rasputin"
BR2_TARGET_GENERIC_ISSUE="Rasputin OS"

# --- kernel (mainline) ---
BR2_LINUX_KERNEL=y
# TODO(scaffold): pin a 6.x mainline kernel + a config with the N100 +
# i915/xe + virtio (for QEMU dev) drivers.

# --- bootloader: GRUB (UEFI) — gives a real boot-attempts counter ---
BR2_TARGET_GRUB2=y
BR2_TARGET_GRUB2_X86_64_EFI=y

# --- A/B + RAUC ---
BR2_PACKAGE_RAUC=y
BR2_PACKAGE_HOST_RAUC=y
BR2_PACKAGE_SQUASHFS=y
BR2_PACKAGE_CRYPTSETUP=y

# --- app primitive ---
BR2_PACKAGE_DOCKER_ENGINE=y
BR2_PACKAGE_DOCKER_CLI=y

# --- Rasputin packages ---
BR2_PACKAGE_RASPUTIN_AGENT=y
BR2_PACKAGE_RASPUTIN_API=y

# --- rootfs ---
BR2_TARGET_ROOTFS_SQUASHFS=y
BR2_TARGET_ROOTFS_SQUASHFS4_XZ=y

# --- overlay + hooks ---
BR2_ROOTFS_OVERLAY="$(BR2_EXTERNAL_RASPUTIN_PATH)/board/rasputin/common/rootfs-overlay"
BR2_ROOTFS_POST_BUILD_SCRIPT="$(BR2_EXTERNAL_RASPUTIN_PATH)/board/rasputin/common/post-build.sh"
BR2_ROOTFS_POST_IMAGE_SCRIPT="$(BR2_EXTERNAL_RASPUTIN_PATH)/board/rasputin/common/post-image.sh"
BR2_ROOTFS_POST_SCRIPT_ARGS="n100"
